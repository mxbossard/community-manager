
{% if params is defined %}
    <a href="{{ path(route, params) }}">
{% else %}
    <a href="{{ path(route) }}">
{% endif %}

    <span class="btn btn-danger">
        <span class="glyphicon glyphicon-cog"></span>
        {{ 'administrate' | trans({}, 'MbyCommunityBundle') | capitalize }}
    </span>
</a>
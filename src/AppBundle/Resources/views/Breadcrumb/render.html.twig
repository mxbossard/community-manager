
<ol class="breadcrumb">
	<li>
		<a href="{{ path('app_publichome_index') }}">
			<span class="glyphicon glyphicon-home" aria-hidden="true"></span>
			{{ 'Home' | trans }}
		</a>
	</li>

	{% for item in items %}
 		<li><a href="{{ path(item) }}">{{ item | trans }}</a></li>
	{% endfor %}
</ol>
